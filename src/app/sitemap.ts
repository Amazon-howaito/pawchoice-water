import type{MetadataRoute}from"next";import{products}from"@/data/products";import{conditions}from"@/data/conditions";import{absoluteUrl}from"@/lib/site";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{const base=["/","/water-fountains","/water-fountains/guide","/water-fountains/faq","/about","/privacy","/disclosure"];return[...base.map(url=>({url:absoluteUrl(url),lastModified:"2026-08-27"})),...products.map(p=>({url:absoluteUrl(`/water-fountains/${p.slug}`),lastModified:p.checkedAt})),...conditions.filter(c=>c.status==="active").map(c=>({url:absoluteUrl(`/water-fountains/${c.slug}`),lastModified:"2026-08-27"}))]}
